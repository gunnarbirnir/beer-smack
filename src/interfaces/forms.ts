
export interface IRoomValues {
  code: string;
  title: string;
  isBlind: boolean;
}

export interface IBeerValues {
  name: string;
  index: number;
  type: string;
  abv: number;
  active: boolean;
  brewer: string;
  country: string;
  description: string;
}